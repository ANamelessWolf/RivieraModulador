<UserControl x:Class="DaSoft.Riviera.Modulador.Bordeo.UI.BordeoPazLuzItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DaSoft.Riviera.Modulador.Bordeo.UI"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid x:Name="container" Background="#FFE6EEF5">
        <Grid.RowDefinitions>
            <RowDefinition  x:Name="FondoHeight" Height="300"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="FrenteWidth" Width="300"/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="CodeTxt" Text="BR2060363024T" VerticalAlignment="Center" TextAlignment="Center"
                   FontSize="25"/>
    </Grid>

</UserControl>
